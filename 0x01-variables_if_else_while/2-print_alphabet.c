#include<stdio.h>
/**
 * main - prgram that prints alphabet in lowercase
 * return: 0 means success.
 */
int main(void)
{
        char x;

        for (x = 'a'; x <='z'; x++)
	putchar(x);
	putchar('\n');
	return (0);
}
