#include<stdio.h>
/**
 * main - prgram that prints alphabet in lowercase
 * Return: means success.
 */
int main(void)
{
	char x;
	for (x = 'a'; x <= 'z'; x++)
	putchar(x);
	putchar('\n');
	return (0);
}
