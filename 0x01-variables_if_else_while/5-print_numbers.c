#include <stdio.h>
#include <Stdlib.h>
#include <time.h>

/**
 * main - entry point
 * Return: always 0
 */

int main(void)
{
	int a;
	for (a = 0; a < 10; a++)
	printf("%d", a);
	printf("\n");

	return (0);
 
}

